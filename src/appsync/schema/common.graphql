scalar AWSDateTime
scalar AWSJSON

type PageInfo {
  hasNextPage: Boolean!
  endCursor: String
}

type MutationResponse {
  success: Boolean!
  message: String
}

input PaginationInput {
  limit: Int
  cursor: String
}
