scalar AWSDateTime
scalar AWSJSON

type PageInfo {
  total: Int
  page: Int
  limit: Int
  hasNextPage: Boolean
  endCursor: String
}

type MutationResponse {
  success: Boolean!
  message: String
}

input PaginationInput {
  limit: Int
  page: Int
  cursor: String
}
